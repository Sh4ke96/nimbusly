/** Set when onboarding completes — avoids a profiles round-trip in middleware. */
export const ONBOARDING_COMPLETE_COOKIE = "nimbusly-onboarding-complete" as const;

export const ONBOARDING_COMPLETE_COOKIE_VALUE = "1" as const;
