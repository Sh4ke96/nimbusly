/** Desktop height for compact header controls (search field, profile chip). */
export const HEADER_CONTROL_HEIGHT = "h-8";

/** Square icon buttons — 44px touch target on mobile, 32px on desktop. */
export const HEADER_ICON_BUTTON_CLASS = "h-10 w-10 sm:h-8 sm:w-8 shrink-0";

export const HEADER_CONTROL_SIZE_PX = 32;
